import { default as public__ } from './public.js'

/**
 * Object [ koorie.resource.get_public ]
 * returns the path of the public file from requests.
 *
 * @returns {string}
 */
export default function resource_get_public() {
    return public__[ 0 ]
}
